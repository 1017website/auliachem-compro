<div class="public-products container">
    <div class="products-heading"><span>{{ ['id'=>'KATALOG','en'=>'CATALOG','zh'=>'产品目录'][$locale] }}</span><h3>{{ ['id'=>'Produk yang dapat kami pasok','en'=>'Products we can supply','zh'=>'我们可供应的产品'][$locale] }}</h3><p>{{ ['id'=>'Spesifikasi dan kemasan dapat disesuaikan dengan kebutuhan pengadaan.','en'=>'Specifications and packaging can be matched to your procurement needs.','zh'=>'规格与包装可按采购需求调整。'][$locale] }}</p></div>
    <div class="product-catalog">@foreach($products as $product)<article class="product-card">@if($product->image_path)<img src="{{ asset('storage/'.$product->image_path) }}" alt="{{ $product->localized('name',$locale) }}" loading="lazy">@endif<div class="product-card-body"><span>{{ \App\Models\Product::CATEGORIES[$product->category] ?? $product->category }}</span><h4>{{ $product->localized('name',$locale) }}</h4>@if($product->localized('summary',$locale))<p>{{ $product->localized('summary',$locale) }}</p>@endif<dl>@foreach([['SKU',$product->sku],['Grade',$product->grade],[['id'=>'Kemasan','en'=>'Packaging','zh'=>'包装'][$locale],$product->packaging]] as [$label,$value])@if($value)<div><dt>{{ $label }}</dt><dd>{{ $value }}</dd></div>@endif @endforeach</dl></div></article>@endforeach</div>
</div>
